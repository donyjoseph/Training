import { colours } from "nodemon/lib/config/defaults";
import{useState} from"react";
const colour=()=>{
 const[count,setcount] =useState(0);
 const increment=()=>{
     setcount (count+);
 }
}
return(
   <button onClick={increment}>changecolour</button>

)
export default colour;